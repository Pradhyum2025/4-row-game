# Frontend Environment Variables for Docker
# This file is used during Docker build (Vite embeds these at build time)

# Backend URL - points to backend service exposed on localhost:5000
# When running in Docker, frontend container accesses backend via host port mapping
VITE_BACKEND_URL=http://localhost:5000

# Optional: explicit WebSocket URL (if not set, derived from VITE_BACKEND_URL)
# VITE_WS_URL=ws://localhost:5000/ws

# Optional: explicit API URL (if not set, uses VITE_BACKEND_URL)
# VITE_API_URL=http://localhost:5000

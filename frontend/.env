# Frontend Environment Variables
# This file is used during Docker build and development
# Vite loads .env.local with higher priority (overrides this file)

# Backend URL - points to backend service
# For Docker: use http://localhost:5000 (backend exposed on host port 5000)
# For remote server: use http://3.108.10.182:5000
VITE_BACKEND_URL=http://3.108.10.182:5000

# Optional: explicit WebSocket URL (if not set, derived from VITE_BACKEND_URL)
# VITE_WS_URL=ws://3.108.10.182:5000/ws

# Optional: explicit API URL (if not set, uses VITE_BACKEND_URL)
# VITE_API_URL=http://3.108.10.182:5000

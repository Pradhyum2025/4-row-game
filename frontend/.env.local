# Frontend Environment Variables (Local Development)
# This file has HIGHEST priority and overrides .env
# Used for local development with npm run dev
# This file is git-ignored and docker-ignored
#
# IMPORTANT: After changing this file, you MUST:
# 1. Stop the dev server (Ctrl+C)
# 2. Clear Vite cache: rm -rf node_modules/.vite (or delete the folder)
# 3. Restart: npm run dev
# 4. Hard refresh browser: Ctrl+Shift+R

# Backend URL for local development
VITE_BACKEND_URL=http://3.108.10.182:5000

# Optional: explicit WebSocket URL (if not set, derived from VITE_BACKEND_URL)
# VITE_WS_URL=ws://3.108.10.182:5000/ws

# Optional: explicit API URL (if not set, uses VITE_BACKEND_URL)
# VITE_API_URL=http://3.108.10.182:5000

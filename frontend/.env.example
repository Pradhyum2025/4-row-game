## 4-row-game Frontend Environment
#
# Copy this file to `.env` or `.env.local` (both are git-ignored)
# for local development, and adjust values as needed.

# Base URL for the backend HTTP API.
# For local development with backend running on port 8080:
VITE_BACKEND_URL=http://3.108.10.182:5000

# Optional: explicit WebSocket URL.
# If not set, the frontend derives it from VITE_BACKEND_URL.
# Example for local backend on 8080:
# VITE_WS_URL=ws://localhost:8080/ws

# Optional: explicit REST API URL.
# If not set, the frontend uses VITE_BACKEND_URL.
# VITE_API_URL=http://localhost:8080


# Backend Environment Variables for Docker
# This file is used by docker-compose.yml

# Server port (mapped to 5000 in docker-compose)
PORT=5000

# Database connection - using DATABASE_URL (preferred)
# Points to postgres service in docker-compose (hostname: postgres)
# Database name matches docker-compose POSTGRES_DB
DATABASE_URL=postgres://postgres:postgres@postgres:5432/connect4?sslmode=disable

# Alternative: Individual PostgreSQL variables (uncomment if not using DATABASE_URL)
# PGUSER=postgres
# PGPASSWORD=postgres
# PGHOST=postgres
# PGPORT=5432
# PGDATABASE=connect4
# PGSSLMODE=disable

# Kafka (optional - backend works without Kafka)
# KAFKA_BROKER=localhost:9092
# KAFKA_TOPIC=game-events

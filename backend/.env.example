## 4-row-game Backend Environment
#
# Copy this file to `.env` (which is git-ignored) and
# fill in the values that make sense for your setup.

# Option 1 (recommended): Single DATABASE_URL
# If set, this is used instead of the individual PG* vars.
# Example for local Postgres:
# DATABASE_URL=postgres://postgres:YOUR_PASSWORD_HERE@localhost:5432/connectfour?sslmode=disable

# Option 2: Individual PostgreSQL parameters
# These are only used if DATABASE_URL is NOT set.
# PGUSER=postgres
# PGPASSWORD=YOUR_PASSWORD_HERE
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=connectfour
# PGSSLMODE=disable

# Kafka (optional - backend works without Kafka; analytics will be disabled if it cannot connect)
# KAFKA_BROKER=localhost:9092
# KAFKA_TOPIC=game-events

# HTTP server port for the backend
# Default is 8080 if not set.
# PORT=8080

